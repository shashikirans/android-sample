pipeline {
    agent {
        docker { 
            image 'mingc/android-build-box:latest'
            }
    }
    stages {
        stage('Test') {
            steps {
                sh 'fastlane --version'
            }
        }
    }
}
